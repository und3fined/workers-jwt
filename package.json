{
  "name": "workers-jwt",
  "version": "0.0.1",
  "description": "Allow use JWT in Cloudflare Workers. Fork from HONO framework.",
  "module": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/und3fined/workers-jwt.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsx ./build.ts",
    "dev": "rimraf dist && tsx ./build.ts --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/node": "^20.5.0",
    "arg": "^5.0.2",
    "esbuild": "^0.19.2",
    "rimraf": "^5.0.1",
    "tsx": "^3.12.7",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
